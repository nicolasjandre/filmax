import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  glassContainer: {
    overflow: "hidden",
    height: 65,
  },
  textsContainer: {
    width: 60,
    gap: 6,
    alignItems: "center",
    justifyContent: "center",
  },
  imdbName: {
    color: "#ffffff"
  },
  imdbRate: {
    fontSize: 28,
    color: "#ffffff",
    textAlign: "center",
  },
});
