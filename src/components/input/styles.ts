import { StyleSheet, Platform } from "react-native";

export const styles = StyleSheet.create({
    container:{
        flexDirection:'row',
    },
    input:{
        height: 48,
        width: 327,
        flex: 1,
        paddingLeft: 40,
        borderRadius: 20,
        fontSize: 18,
        marginTop: 431,
        marginLeft: 24,
    }
})