import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#15141F",
        padding: 16,
        paddingBottom: 0,
        width: "100%",
    },
    img: {
        marginTop: 20,
        marginBottom: 30,
        width: 120,
        height: 25,
    },
});
