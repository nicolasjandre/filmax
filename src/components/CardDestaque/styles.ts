import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: 210,
    width: 350,
    borderRadius: 40,
    overflow: "hidden",
    marginBottom: 20
  },
  image: {
    flex: 1,
  },
});
