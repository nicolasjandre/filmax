import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  loginButton: {
    marginTop: "5%",
    backgroundColor: "#FFF",
    width: "90%",
    height: 42,
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 20,
  },
  loginText: {
    color: "#000000",
    fontSize: 17,
  }
});
