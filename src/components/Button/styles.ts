import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  loginButton: {
    marginTop: "5%",
    backgroundColor: "#FFF",
    width: "80%",
    height: 50,
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 20,
  },
  loginText: {
    color: "#000000",
    fontSize: 18,
    fontWeight: "bold",
  },
});
