import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#15141F",
    padding: 16,
    paddingTop: 30,
    paddingBottom: 0,
    alignItems: "center",
  },
  text: {
    color: "#ffffff",
  },
});
